---
title: "Академическое руководство по разработке Shekinah Mission Cloud"
description: "Полное руководство по созданию и настройке сайта с нуля"
publishDate: "2026-02-05"
tags: ["development", "guide", "astro", "tailwind"]
---

# 📚 Академическое руководство по разработке Shekinah Mission Cloud

## 🎯 Введение

Данное руководство описывает полный процесс разработки сайта Shekinah Mission Cloud на базе Astro framework с использованием Tailwind CSS. Руководство охватывает все этапы от базовой настройки до реализации продвинутых функций.

---

## 🏗️ Архитектура проекта

### Структура директорий

```
src/
├── components/          # Компоненты UI
├── layouts/            # Макеты страниц
├── pages/              # Страницы сайта
├── styles/             # Стили CSS
└── data/               # Данные контента
```

---

## 🎨 Глава 1: Стилизация главной страницы

### 1.1 Двухцветный заголовок

**Файл:** `/src/pages/index.astro`

```astro
<h1 class="text-4xl md:text-5xl font-bold bg-gradient-to-r from-blue-600 via-purple-600 to-indigo-600 bg-clip-text text-transparent mb-6">
  🌍 Shekinah Mission Cloud
</h1>
```

**Объяснение классов:**
- `text-4xl md:text-5xl` - адаптивный размер текста
- `font-bold` - жирное начертание
- `bg-gradient-to-r` - градиент слева направо
- `from-blue-600 via-purple-600 to-indigo-600` - цвета градиента
- `bg-clip-text text-transparent` - обрезка градиента по тексту

### 1.2 Оформление цитат

**Исходный код:**
```astro
<blockquote class="hero-quote relative p-6 my-8 bg-gradient-to-r from-indigo-500 to-purple-600 text-white rounded-xl shadow-lg">
  <!-- Содержимое цитаты -->
</blockquote>
```

**Модифицированный код:**
```astro
<p class="text-xl md:text-2xl text-gray-700 dark:text-gray-300 mb-4">
  <!-- Текст цитаты -->
</p>
<p class="text-lg text-orange-500 font-medium">
  — Миссия Шехина & Веб-Студия
</p>
```

**Причина изменений:** Удалены фоновые стили для создания чистого текстового оформления.

---

## 🔄 Глава 2: Пагинация блога

### 2.1 Основная страница блога

**Файл:** `/src/pages/blog/index.astro`

```astro
---
import { getCollection } from 'astro:content'

// Настройки пагинации
const postsPerPage = 10;
const allPosts = await getCollection('posts');
allPosts.sort((a, b) => new Date(b.data.publishDate) - new Date(a.data.publishDate));

// Получение текущей страницы
const currentPage = Astro.url.searchParams.get('page') ? 
  parseInt(Astro.url.searchParams.get('page')) : 1;
const totalPages = Math.ceil(allPosts.length / postsPerPage);
const startIndex = (currentPage - 1) * postsPerPage;
const endIndex = startIndex + postsPerPage;
const currentPosts = allPosts.slice(startIndex, endIndex);
---
```

### 2.2 Динамические страницы пагинации

**Файл:** `/src/pages/blog/[page].astro`

```astro
---
export async function getStaticPaths() {
  const allPosts = await getCollection('posts');
  allPosts.sort((a, b) => new Date(b.data.publishDate) - new Date(a.data.publishDate));
  
  const postsPerPage = 10;
  const totalPages = Math.ceil(allPosts.length / postsPerPage);
  
  const paths = [];
  for (let i = 2; i <= totalPages; i++) {
    paths.push({
      params: { page: i.toString() },
      props: { 
        currentPage: i,
        totalPages: totalPages,
        allPosts: allPosts,
        postsPerPage: postsPerPage
      }
    });
  }
  
  return paths;
}
---
```

**Принцип работы:**
- `getStaticPaths()` генерирует статические страницы для каждой страницы пагинации
- URL структура: `/blog/2`, `/blog/3` и т.д.
- Автоматическая генерация при добавлении новых постов

---

## 🔘 Глава 3: Кнопки скроллинга

### 3.1 Компонент кнопок

**Файл:** `/src/components/GlobalScrollButtons.astro`

```astro
---
// Глобальные кнопки скроллинга которые работают на всех страницах
---

<div id="globalScrollButtons" class="scroll-buttons">
  <button id="globalScrollUp" class="scroll-btn scroll-up" title="Наверх" onclick="scrollUp()">
    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor">
      <path d="m18 15-6-6 6"/>
    </svg>
  </button>
  <button id="globalScrollDown" class="scroll-btn scroll-down" title="Вниз" onclick="scrollDown()">
    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor">
      <path d="m6 9 6 6 6-6"/>
    </svg>
  </button>
</div>
```

### 3.2 Стили кнопок

```css
.scroll-buttons {
  position: fixed;
  right: 1rem;
  top: 50%;
  transform: translateY(-50%);
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
  z-index: 9999;
}

.scroll-btn {
  width: 2rem;
  height: 2rem;
  border: none;
  border-radius: 50%;
  background: linear-gradient(135deg, #3b82f6 0%, #1e40af 100%);
  color: white;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 2px 8px rgba(59, 130, 246, 0.2);
  transition: all 0.2s ease;
  opacity: 0.8;
}

.scroll-btn:hover {
  background: linear-gradient(135deg, #2563eb 0%, #1e3a8a 100%);
  transform: scale(1.05);
  opacity: 1;
}
```

### 3.3 Глобальные функции скроллинга

**Файл:** `/src/layouts/BaseLayout.astro`

```astro
<html lang="ru">
<head>
  <BaseHead title={title} description={description} permalink={permalink} />
  <script>
    // Глобальные функции для скроллинга
    window.scrollUp = function() {
      window.scrollTo({ top: 0, behavior: 'smooth' });
    };
    
    window.scrollDown = function() {
      window.scrollTo({ top: document.body.scrollHeight, behavior: 'smooth' });
    };
  </script>
</head>
```

**Почему это работает:**
- Функции определяются в `head` и доступны глобально
- `onclick` обработчики вызывают глобальные функции
- Работает при SPA навигации Astro

---

## 🛠️ Глава 4: Управление и развитие

### 4.1 Добавление новых постов в блог

1. Создайте файл в `/src/data/blog-posts/имя-поста.mdx`
2. Добавьте frontmatter:

```yaml
---
title: "Название поста"
publishDate: "2026-02-05"
description: "Краткое описание"
tags: ["тег1", "тег2"]
---
```

3. Пагинация обновится автоматически

### 4.2 Настройка кнопок скроллинга

**Изменение размера:**
```css
.scroll-btn {
  width: 2.5rem;  /* Увеличить */
  height: 2.5rem;
}
```

**Изменение положения:**
```css
.scroll-buttons {
  right: 2rem;  /* Отодвинуть дальше */
  bottom: 2rem; /* Переместить вниз */
  top: auto;
  transform: none;
}
```

### 4.3 Модификация пагинации

**Изменение количества постов на странице:**
```astro
const postsPerPage = 15;  // Вместо 10
```

**Изменение стилей пагинации:**
```astro
<a href={`/blog/${currentPage + 1}`} class="pagination-btn" 
   style="padding: 0.75rem 1.5rem; background: #1e40af;">
  Вперед →
</a>
```

---

## 📋 Глава 5: Лучшие практики

### 5.1 Структура компонентов

- **Компоненты** должны быть независимыми
- **Стили** инкапсулированы в компонентах
- **Глобальные функции** только при необходимости

### 5.2 Оптимизация производительности

- **Статическая генерация** для пагинации
- **Ленивая загрузка** изображений
- **Минимизация CSS** через Tailwind

### 5.3 Доступность

- **Атрибуты title** для кнопок
- **Семантическая HTML** разметка
- **Контрастные цвета** для читаемости

---

## 🚀 Глава 6: Тестирование и отладка

### 6.1 Проверка функциональности

1. **Кнопки скроллинга:**
   - Проверьте на разных страницах
   - Проверьте при навигации
   - Проверьте на мобильных устройствах

2. **Пагинация:**
   - Проверьте навигацию между страницами
   - Проверьте правильность URL
   - Проверьте отображение постов

### 6.2 Инструменты отладки

- **Консоль браузера** для ошибок JavaScript
- **Network tab** для проверки загрузки ресурсов
- **Astro dev server** для локальной разработки

---

## 📚 Заключение

Данное руководство предоставляет полный набор инструкций для разработки и поддержки сайта Shekinah Mission Cloud. Следуя этим принципам, вы можете легко расширять функциональность и поддерживать код в чистом состоянии.

---

## 🙏 Благодарности

Разработка выполнена с использованием современных веб-технологий и лучших практик. Благодарю Высший Интеллект за мудрость и понимание.

---

*© 2026 Shekinah Mission Cloud. Все права защищены.*
