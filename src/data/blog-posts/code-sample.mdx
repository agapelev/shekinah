---
title: '📝 Пример Markdown с кодом'
slug: code-sample
publishDate: 2026-01-01
description: 'Пример Markdown-файла с блоками кода и подсветкой синтаксиса'
tags: ["markdown", "код", "guide"]
---

Пример поста с использованием Markdown.

## 🔤 Встроенное выделение кода

Пример встроенного выделения кода: `сумма = parseInt(число1) + parseInt(число2)`

## 💻 Блоки кода

### JavaScript код

```javascript
var число1, число2, сумма
число1 = prompt('Введите первое число')
число2 = prompt('Введите второе число')
сумма = parseInt(число1) + parseInt(число2) // "+" означает "сложить"
alert('Сумма = ' + сумма) // "+" означает объединение в строку
```

### Python код 🐍

```python
def числа_фибоначчи():
    a, b = 0, 1
    while True:            # Первая итерация:
        yield a            # возвращаем 0 для начала, а затем
        a, b = b, a + b    # теперь a = 1, и b = 1 (0 + 1)

# Выводим первые 10 чисел Фибоначчи
for индекс, число_фибоначчи in zip(range(10), числа_фибоначчи()):
     print('{i:3}: {f:3}'.format(i=индекс, f=число_фибоначчи))
```

### HTML разметка

```html
<!DOCTYPE html>
<html>
  <head>
    <title>Пример страницы</title>
    <meta charset="utf-8" />
  </head>
  <body>
    <h1>Привет, мир!</h1>
    <p>Это пример HTML-страницы.</p>
  </body>
</html>
```

### CSS стили

```css
/* Стили для основного контейнера */
.контейнер {
  width: 100%;
  max-width: 1200px;
  margin: 0 auto;
  padding: 20px;
}

/* Стили для заголовков */
h1 {
  color: #2c3e50;
  font-family: 'Arial', sans-serif;
}

/* Адаптивные стили */
@media (max-width: 768px) {
  .контейнер {
    padding: 10px;
  }
}
```
